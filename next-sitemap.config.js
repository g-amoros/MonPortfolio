/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://portfolio.gael-amoros.fr',
    generateRobotsTxt: true, // (optional)
    // ...other options
}